#include <stdio.h>

int main(){

    // how to define an array in C?
    int marks[5];  

    marks[0] = 80;
    marks[1] = 60;
    marks[2] = 70;
    marks[3] = 85;
    marks[4] = 89;

    printf("index 3 of array marks %d\n", marks[3]);

    return 0;
}