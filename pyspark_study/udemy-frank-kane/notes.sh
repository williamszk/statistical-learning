
python -m venv .venv

where java

# where we installed spark
cd c:\spark

# If everything went ok when setting the environment variables
# this command will work:
pyspark

# install pyspark in the environment
pip install pyspark

# to run a .py script inside spark
spark-submit ratings_counter.py